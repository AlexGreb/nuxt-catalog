# Каталог товаров на Nuxt + Pinia + Tailwind


## Установка

```bash

npm install

```

## Заупуск сервера

Запускает сервер по `http://localhost:3000` и json-server на `http://localhost:3001`:

```bash

npm run dev

```
